{
    "name": "k8sdockside-optimization",
    "version": "1.0.0",
    "private": true,
    "description": "Optimization advisor: a K8s Dockside plugin that checks a cluster for resource, reliability, scaling and waste problems, scores it, and says what to change.",
    "license": "Apache-2.0",
    "author": "K8s Dockside",
    "repository": {
        "type": "git",
        "url": "https://github.com/k8sdockside/optimization"
    },
    "type": "module",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "k8sdockside-plugin build",
        "watch": "k8sdockside-plugin watch",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "check": "npm run typecheck && npm run test && k8sdockside-plugin check"
    },
    "devDependencies": {
        "@k8sdockside/plugin-sdk": "^1.0.0",
        "typescript": "^6.0.3",
        "vitest": "^4.1.11"
    }
}
